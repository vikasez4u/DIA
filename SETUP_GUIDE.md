# Digital Inclusivity Auditor - Setup Requirements
## Quick Setup Instructions:

1. Clone the repository
2. Run `python setup.py` - installs all dependencies and initializes database
3. Run `python backend/api/app.py` - starts the server
4. Frontend setup with `npm install && npm start`
